sudo: required

services:
  - docker

language: node_js
node_js: 
  - "stable"

script: 
   - cd lykke-log  && npm install
   - cd .. && cd test && npm install
   - cd.. && ./create-test-volume.sh
   - ./create-test-network.sh
   - ./test.sh


